package commandPattern.simpleRemoteControllerTest;

import commandPattern.controller.*;
import commandPattern.controller.garage.GarageDoor;
import commandPattern.controller.garage.GarageDoorOpenCommand;
import commandPattern.controller.light.Light;
import commandPattern.controller.light.LightOnCommand;

public class RemoteControlTest {
    public static void main(String[] args) {
        /*SimpleRemoteControl simpleRemoteControl = new SimpleRemoteControl();
        Command cmd = new LightOnCommand(new Light("Kitchen"));

        simpleRemoteControl.setSlot(cmd);
        simpleRemoteControl.buttonWasPressed(); // 버튼을 눌렀을 뿐인데 Light 객체의 on이 눌렸다.

        Command cmd2 = new GarageDoorOpenCommand(new GarageDoor("hello garage"));
        simpleRemoteControl.setSlot(cmd2);
        simpleRemoteControl.buttonWasPressed();*/
    }
}
